# 🔧 BACKFILL: Corrigir Vencedores do Dia 16/12/2025

## ❌ Problema

**Erro:** "Você NÃO é o vencedor registrado on-chain para este rank"

**Causa:** Vencedores do dia 16/12/2025 (UTC) não foram registrados no contrato.

---

## ✅ Solução (Passo Único)

### 1️⃣ Configure os vencedores

Edite `scripts/backfill-2025-12-16.ts` e substitua os endereços:

```typescript
const winners = [
  "0xPRIMEIRO_COLOCADO", // rank 1 - SUBSTITUA pelo endereço correto
  // Se só teve 1 jogador, deixe só esse
  // Se teve mais:
  // "0xSEGUNDO_COLOCADO", // rank 2 (opcional)
  // "0xTERCEIRO_COLOCADO", // rank 3 (opcional)
]
```

### 2️⃣ Execute o script

```bash
npm run backfill-2025-12-16
```

---

## 🎯 Resultado Esperado

Após executar:

- ✅ `getWinner(day, rank)` retorna exatamente a wallet do jogador
- ✅ Botão "Reivindicar Prêmio" habilita
- ✅ `claimPrize(day, rank)` FUNCIONA
- ✅ Erro NÃO volta

---

## 🚨 IMPORTANTE (NÃO PULAR)

- ⚠️ **Rodar apenas UMA VEZ**
- ⚠️ **Usar owner do contrato** (wallet configurada em `PRIZE_POOL_OWNER_PRIVATE_KEY`)
- ⚠️ **NÃO rodar de novo para o mesmo dia** (script verifica se já está registrado)
- ✅ **O frontend não precisa mudar nada**

---

## 📋 O que o script faz:

1. Verifica se a wallet é owner do contrato
2. Calcula `day` para 16/12/2025 UTC
3. Verifica se vencedores já estão registrados (não duplica)
4. Valida todos os endereços
5. Chama `setDailyWinnersArray(day, winners)`
6. Verifica se os vencedores foram registrados corretamente

---

## 🟢 Daqui pra frente

Execute TODO DIA após virar UTC:

```bash
npm run register-winners-auto
```

Ou configure agendamento automático (ver `AGENDAR_REGISTRO_AUTOMATICO.md`).

